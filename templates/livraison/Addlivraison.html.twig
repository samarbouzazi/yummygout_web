{% extends 'base-back.html.twig'%}
{% block body %}
    <div style="margin-left:350px; margin-top: 100px">
        <a href="{{ path('Affichel') }}">Retour à la liste</a>
        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
        <h1>Ajouter livraison</h1>
        <div class="col-md-9" >
            {% for message in app.flashes('alerte')  %}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>{% endfor %}</div>
            <div class="col-md-9" >
                {{ form_widget(form.reflivraison , {'attr':{'class':'form-control'}})}}
                {{ form_errors(form.reflivraison) }}
            </div>
           <br>
            <div class="col-md-9">
                {{ form_widget(form.idpanier , {'attr':{'class':'form-control'}}) }}
                {{ form_errors(form.idpanier) }}
            </div>
            <br>
            <div class="col-md-9">
                {{ form_widget(form.idlivreur,  {'attr':{'class':'form-control'}}) }}
                {{ form_errors(form.idlivreur) }}
            </div>
            <div class="col-md-9">
                <br>
                {{ form_widget(form.Ajouter, {'attr':{'class':'btn btn-primary'}}) }}
            </div>

        {{ form_end(form) }}
    </div>
{% endblock %}